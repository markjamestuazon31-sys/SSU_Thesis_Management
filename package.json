{
  "name": "ssu-thesis-management-professional",
  "version": "7.0.0",
  "private": true,
  "type": "module",
  "description": "Professional SSU CAS Thesis Record Management System with Firebase Realtime Database manuscript versioning, external adviser document review, reviewed-file return workflow, review drafts, final administrator approval, and no Firebase Storage.",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "check": "node scripts/check.mjs",
    "check:indexes": "node scripts/check-indexes.mjs",
    "deploy:rules": "npx firebase-tools deploy --only database --project inventory-61ec5"
  },
  "dependencies": {
    "firebase": "^12.0.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
