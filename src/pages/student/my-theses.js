import { getMyTheses } from '../../services/thesis.service.js';
import { pageHeader, thesisTable } from '../../components/ui.js';
export async function render({profile}){const rows=await getMyTheses(profile.uid);return `${pageHeader('My Thesis Records','Monitor manuscript status, Research Instructor feedback, revisions, approval, and publication.','<a class="btn btn-primary" href="#/student/submit">+ New submission</a>')}<section class="panel"><div class="panel-body no-pad">${thesisTable(rows,{showResearchInstructor:true,actionRoute:id=>`/student/thesis/${id}`})}</div></section>`;} export function mount(){}
