import { getSystemSettings, updateSystemSettings } from '../../services/settings.service.js';
import { pageHeader } from '../../components/ui.js';
import { escapeHtml, formDataObject, setButtonLoading } from '../../utils/dom.js';
import { toast } from '../../components/toast.js';
export async function render(){const s=await getSystemSettings();return `${pageHeader('System Settings','Configure thesis submission and repository defaults.')}<section class="panel form-panel"><div class="panel-body"><form id="system-form" class="form-stack"><label class="field"><span>Academic year</span><input name="academicYear" value="${escapeHtml(s.academicYear||'')}" required></label><label class="field"><span>Repository title</span><input name="repositoryTitle" value="${escapeHtml(s.repositoryTitle||'')}" required></label><label class="switch-row"><div><strong>Accept new thesis submissions</strong><span>Students can create new thesis records while enabled.</span></div><input name="submissionOpen" type="checkbox" ${s.submissionOpen?'checked':''}></label><button class="btn btn-primary" type="submit">Save settings</button></form></div></section>`;}
export function mount(){document.getElementById('system-form')?.addEventListener('submit',async e=>{e.preventDefault();const btn=e.currentTarget.querySelector('button');setButtonLoading(btn,true,'Saving...');try{const d=formDataObject(e.currentTarget);await updateSystemSettings({academicYear:d.academicYear,repositoryTitle:d.repositoryTitle,submissionOpen:e.currentTarget.elements.submissionOpen.checked});toast('System settings saved.','success');}catch(err){toast(err.message,'error');}finally{setButtonLoading(btn,false);}});}
